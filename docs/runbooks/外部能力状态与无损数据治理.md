# 外部能力状态与无损数据治理

## 1. 状态口径

配置生命周期与运行可用性必须分开显示。任何页面、接口说明或验收报告都不得把“配置已启用”
写成 `ONLINE`，也不得把本地配置存在写成外部能力已验证。

| 能力                             | 未配置或无证据时的唯一允许口径     | 只有何时可以提升                                                             |
| -------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------- |
| 飞书目录                         | 未配置 / 同步不可用                | 新 Secret 在线换取 Token、部门与成员权限均通过；完成同步后才显示本次同步成功 |
| Manus                            | 未配置 / 不可用 / 供应商仅返回终态 | 真实 Run 回执通过；只有供应商提供多个真实 delta 才能声明增量流式             |
| Embedding                        | 未配置 / 语义未就绪 / 不可连接     | 非空已发布切片由指定模型生成真实向量且覆盖率为 100%                          |
| Reranker                         | 未配置 / 语义未就绪 / 不可连接     | 真实候选经过指定 Reranker，企业问题集与延迟门禁通过                          |
| 企业 IdP / 邮件 / 对象存储 / OCR | 未配置 / 不可用                    | 真实外部环境完成签名、投递、存取或解析验收                                   |
| OTLP / SIEM                      | 未配置 / 导出不可用 / 导出证据过期 | Collector/SIEM 收到最新成功导出并且未超过 stale 窗口                         |

智能体记录中的 `ONLINE` 是历史数据库配置枚举，只允许在用户界面翻译为“配置已启用”。员工端
是否开放联系必须继续复核 Runtime、可信路由、精确允许列表、熔断器和近期真实成功回执。配置已
启用但上述证据缺失时，入口应禁用并显示具体阻断原因。

知识检索的 `HYBRID` 只允许在非空已发布切片、当前指定 Embedding 模型覆盖率 100% 且 Reranker
就绪时出现。空集合、任意历史向量或只启用开关都不能通过门禁。

### 1.1 2026-07-29 本轮验收边界

- 全新隔离数据库已部署 87 个 migration，PostgreSQL 154/154、JS/TS 1711、
  AI Runtime 246、语义验收 27/27 通过；Typecheck、Lint、Prettier 和生产构建通过；
- 备份 SHA-256 为
  `211fe10add482c7cc6f033c5cdb2b4dbe3e57acd8a71b55dbfbf8c7a6a99e1e7`，
  两次隔离恢复均为 `restore-verified`，恢复门禁 34/34 通过；
- 独立验收服务位于 API `3300`、Admin preview `4273`、Admin dev `4274`、
  AI Runtime `8200`；原有 `3000/4173/8100` 服务未停止、重启或修改；
- `enterprise_agent_acceptance` 与 `enterprise_agent_vector_acceptance` 未访问或修改；
- 浏览器/Electron UAT 尚未完成；真实 Manus 增量流与逐 Run Token、Embedding、Reranker、
  飞书、企业 IdP、邮件、对象存储、OCR、OTLP/SIEM 仍未联网验收；
- 当前结论为仓库自动化与隔离恢复门禁通过、企业生产 **No-Go**。

## 2. 飞书 Secret 轮换阻断

曾出现在聊天、工单、截图、日志或源码中的 App Secret 已视为泄露，不得再次粘贴到管理端、环境
变量或验收脚本。真实联网验收前必须：

1. 在飞书开放平台轮换同一 App ID 的 Secret；
2. 清理旧 Secret 的环境变量和 Secret Manager 版本；
3. 只把新 Secret 注入服务端凭据入口，不进入命令行参数、日志、数据库报告或客户端；
4. 在管理端确认“新生成且未暴露”后执行在线权限探测；
5. 记录轮换时间、执行人和变更单，不记录 Secret 原文。

管理端的确认框是人为防误用门禁，不是 Secret 泄露检测器。直接 API 调用和部署环境仍必须由
Secret Manager、最小权限和轮换流程约束。

## 3. 隔离恢复库只读治理评估

[`Invoke-EnterpriseDataGovernanceDryRun.ps1`](../../infra/scripts/Invoke-EnterpriseDataGovernanceDryRun.ps1)
只运行 `READ ONLY` 事务并以 `ROLLBACK` 结束。它不会更新、删除、复制或重放任何事件，也不会
释放 `UNKNOWN` Run 的额度占位。脚本评估：

- Outbox 路由注册、fact-only 映射、缺失 delivery 和隔离事件；
- 已到可信终态但仍保留预留额度的上限结算候选，以及必须继续保守占位的 `UNKNOWN` Run；
- `ONTOLOGY.MAPPING.MISSING` 实例冲突按 predicate、subject type、object type 聚合后的
  schema-gap 数量；
- 非当前 READY 文档版本及其图谱证据，作为候选/历史投影回填的待人工分类集；
- 当前迁移集是否已提供图谱投影生命周期与 schema-gap 聚合能力。

运行前必须先使用仓库备份和恢复脚本，验证备份 SHA-256，并把备份恢复到名称含
`governance`、`repair`、`restore` 或 `rehearsal` 的独立数据库。脚本拒绝两个受保护验收库，
也拒绝直接连接备份源数据库。

直接 PostgreSQL 客户端示例：

```powershell
$env:PGPASSWORD = '<从本机受控凭据源读取，不写入脚本>'
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\infra\scripts\Invoke-EnterpriseDataGovernanceDryRun.ps1 `
  -PgBinDirectory 'C:\PostgreSQL\16\bin' `
  -DatabaseHost 127.0.0.1 `
  -Port 55445 `
  -Database enterprise_agent_restore_defect_fix_20260729_restore_rehearsal `
  -DatabaseUser postgres `
  -TenantId 00000000-0000-7000-8000-000000000001 `
  -BackupManifestPath .\.data\backups\defect-fix-20260729\enterprise_agent_defect_fix_final_20260729_1140-20260729T035630Z-43898d1556234bb69e7145708ebaefe8.manifest.json `
  -OutputPath .\.data\backups\defect-fix-20260729\enterprise-data-governance-dry-run.json
```

Docker 示例：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass `
  -File .\infra\scripts\Invoke-EnterpriseDataGovernanceDryRun.ps1 `
  -Container enterprise-agent-postgres-rehearsal `
  -Database enterprise_agent_rehearsal_20260729_001 `
  -DatabaseUser postgres `
  -TenantId 00000000-0000-7000-8000-000000000001 `
  -BackupManifestPath .\.data\backups\uat.manifest.json
```

## 4. 从 dry-run 到受控修复

当前脚本故意不提供 `-Apply`。本轮已经在
`enterprise_agent_restore_defect_fix_20260729_restore_rehearsal` 完成只读演练，结果为
`read-only-dry-run-complete`、`transactionReadOnly=true`、`mutationsPerformed=0`、
`historyDeleted=false`。当前 Seed 租户没有候选、无效、隔离或 schema-gap 待治理记录；
这个空结果只证明演练没有修改数据，不能替代真实存量分类，也不能授权手工 SQL 更新源环境。

后续受控修复只能通过迁移集提供的、可重放且幂等的数据库命令或受信后台服务执行，并满足：

- 先在隔离恢复库完整演练，再对指定租户执行；
- 每个修复类别记录不可变 Audit Event，包含变更单、操作者、前后计数和算法版本；
- 不删除历史 Outbox、Run、关系、冲突、文档版本或审计记录；
- fact-only 只修改路由分类，不伪造投递成功；DELIVERY 继续沿用原 Event ID；
- FAILED/UNKNOWN 不批量改成功、不复制事件、不盲目重放；
- `UNKNOWN` Run 在可信对账前继续占用并发和预留；
- 候选/历史版本先分类再回填投影生命周期，不能把非当前 READY 版本自动视为 ACTIVE；
- schema-gap 聚合保留原实例冲突和关系证据，仅新增聚合治理视图或记录。

只有 dry-run、隔离 Apply 演练、审计链复算、RLS/ACL 与备份恢复复验全部通过，才允许创建面向
源环境的受控修复命令。

## 5. Agent Run UNKNOWN 与回答槽位冲突处理

`UNKNOWN` 表示供应商任务可能仍在执行。墙钟时间、网络断开或本地 Worker 重启都不能证明远端
已经停止，因此不得用定时 SQL 批量把 UNKNOWN 改为 FAILED、CANCELLED 或 QUEUED。

用户侧恢复路径：

1. 先刷新并等待系统/管理员对同一个外部 Run 做可信对账；同会话后续问题会可靠排队。
2. 用户决定不再等待时，使用消息页“结束本次等待”。系统记录
   `FAILED / AI_RUNTIME_RESULT_ABANDONED`、审计事件和保守额度结算，但不声称远端已经取消。
3. 界面必须提醒：远端任务仍可能完成并产生费用。不得自动重放被放弃的原输入；已有独立
   `inputMessageId` 的后续问题可恢复排队。重做原问题必须由用户再次明确发起。

数据库出现 `P2002` 且目标为 `agent_runs_one_answer_per_input_message_idx` 时，表示同一
`(tenant_id, input_message_id)` 已有 `QUEUED / DISPATCHING / RUNNING / UNKNOWN / SUCCEEDED`
槽位持有者。这是防止重复回答的保护，不是应该绕过的数据库故障。处理顺序为：

1. 只读检查该输入消息的全部 Run、状态、`retry_of_run_id`、时间和审计，不批量改状态。
2. 已成功则不重放；在途则等待；UNKNOWN 走可信对账或用户结束等待；FAILED/CANCELLED 且没有
   其他槽位持有者时，使用正式重试 API。
3. 不执行“把所有历史 FAILED 改回 QUEUED”的修复。即使只挑最新一条，也必须先确认没有成功、
   在途或 UNKNOWN 持有者，并优先走 API 状态机与审计。

Agent Run 的 Outbox `attempts` 当前只是领取审计计数，没有 `maxAttempts` 静默死信阈值。高次数
应触发积压与租约诊断；若未来引入上限，必须新增独立的真实执行失败计数，不能因会话排队、容量
延期或租约接管耗尽业务重试预算。

已有 `external_run_id` 的 Agent Run 首次进入 `UNKNOWN` 时，系统会在同一事务创建延时对账
delivery。默认 5 分钟后由 Agent Run Worker 取得租约，执行以下固定流程：

1. 在租户配额锁和 Run 锁内确认状态仍为 `UNKNOWN`、外部 ID 仍存在；
2. 只查询同一个 `external_run_id`，禁止调用 Runtime create；
3. 有可信终态则收敛原 Run；仍不明确则保持 `UNKNOWN` 并创建下一条延时对账事件；
4. 没有外部 ID 时不自动猜测或重放，转人工供应商侧核验。

可通过 `AGENT_RUN_UNKNOWN_RECONCILIATION_DELAY_MS` 调整间隔，生产不得低于配置校验下限。排查时查看
`agent.run.reconciliation_scheduled`、`agent.run.reconciliation_started` 审计以及对应
`outbox_event_deliveries`。当前只有固定间隔和审计，还没有指数退避、抖动及专项告警面板；长期
不可达的 Run 必须纳入灰度告警，不能无限静默探测。

`SUCCEEDED / FAILED / CANCELLED` 是不可逆终态，数据库触发器
`agent_runs_terminal_status_guard` 会拒绝改成其他状态。`UNKNOWN -> RUNNING` 只用于查询原外部任务的
可信对账。禁止 DBA 用直接 SQL 把 `FAILED / AI_RUNTIME_RESULT_ABANDONED` 改为成功或排队；正式重试
创建新 Run。用户已结束等待后，即使延时对账 delivery 随后到期，Worker 也只收口 delivery，不查询
Runtime、不追加消息。将来若供应商主动推送该任务的迟到费用/结果，只能记独立审计和计费事实，
不得改写原 Run 终态或污染会话上下文。
